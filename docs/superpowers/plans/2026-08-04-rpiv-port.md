# rpiv-*2.4.0 → @schovest/pi-* 移植实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 将 @juicesharp/rpiv-btw/todo/ask-user-question 2.4.0 全量移植为 @schovest/pi-* 六个包（含 pi-config/pi-i18n 依赖包与 pi-test-utils 测试夹具包），65 个上游测试全绿。

**Architecture:** 源码从上游仓库 `/tmp/rpiv-mono`（rpiv-mono main = npm 2.4.0，已验证一致）按包复制到 `packages/`，批量替换 `@juicesharp/rpiv-*` 导入为 `@schovest/pi-*`，逐包适配 package.json（lockstep 0.1.2、types/exports 仓库约定），test/setup.ts 裁剪重写为仅覆盖本 5 个运行时包。

**Tech Stack:** TypeScript（strict，Node16 解析）、vitest 4、typebox、@earendil-works/pi-* 0.80.5、biome 1.9.4（不动）。

**上游源码源：** `/tmp/rpiv-mono`（浅克隆）。若该目录缺失，先执行：

```bash
cd /tmp && rm -rf rpiv-mono && git clone --depth 1 https://github.com/juicesharp/rpiv-mono.git rpiv-mono
```

## Global Constraints

- 包名/导入：`@juicesharp/rpiv-*` → `@schovest/pi-*`（全部源码 + 测试 + README + 注释）
- 相对导入后缀：**保留上游 `.js`**（如 `./config.js`），不改成 `.ts`
- 字符串字面量 `"rpiv-todo"` / `"rpiv-ask-user-question"` / `"rpiv-btw"` / `"rpiv-todos"` / `"rpiv-ask-user-question-"` **保持不变**（配置路径、Symbol.for、组件 key、临时目录前缀——改了就破坏行为/迁移）
- LICENSE：MIT 原样复制，保留上游版权署名（juicesharp）
- 上游测试全部搬运，不新写测试
- 工具链：vitest `^4.1.10`、typescript `^6.0.3`、@types/node `22.20.1`、`@earendil-works/pi-*` 统一 `0.80.5`；biome `1.9.4` 与 `biome.json` **不动**
- Lockstep 版本 `0.1.2`；`pi-test-utils` 标 `private: true` 不发布
- 仓库约定：published 包补 `"types"` + `"exports"`（.ts 直出，参照 goal/tps 的 exports 形态）
- 成功标准：`npm run check` 零错误、`npm test` 全绿、`npm run publish:dry` 通过
- **对 spec 的技术修正（D1 偏差）**：spec 原定"现有包保留 pi-*pin（0.74.2/0.79.10）"。但 vitest 的 `vi.mock` 按**解析后的模块路径**注册拦截；setup.ts 位于仓库根，`vi.mock("@earendil-works/pi-ai")` 解析 root 副本，而 packages/pi-btw 内测试 import 同一 specifier 若命中嵌套的 0.80.5 副本，mock 不生效。因此**全仓统一** pi-* 为 0.80.5（现有 4 包 devDeps 一并 bump，根 devDeps 显式声明钉住 hoist 版本）。现有包只用少量 ExtensionAPI 面（已在运行时 pi 0.81.1 验证），0.79.10→0.80.5 预期无破坏，如有 tsc 报错则最小修复。

---

### Task 1: 工具链与 SDK 版本统一

**Files:**

- Modify: `package.json`（root devDeps）
- Modify: `packages/goal/package.json`、`packages/tps/package.json`、`packages/sudo-helper/package.json`（devDeps）

**Interfaces:**

- Produces: 全仓 `@earendil-works/pi-*` 解析为 0.80.5（根 node_modules），vitest 4 / TS 6 可运行

- [ ] **Step 1: 基线验证**

```bash
cd /data/mine/pi-package-mono && npm run check && npm test
```

预期：check 通过；test 显示 pass（当前 0 个测试，passWithNoTests）。

- [ ] **Step 2: 修改 root package.json devDeps**

编辑 `package.json`，将 devDependencies 中：

- `"@types/node": "^22.0.0"` → `"22.20.1"`
- `"@vitest/coverage-v8": "^3.0.0"` → `"^4.1.10"`
- `"typescript": "^5.7.0"` → `"^6.0.3"`
- `"vitest": "^3.0.0"` → `"^4.1.10"`

并新增三条（钉住 hoist 版本，镜像上游 root devDeps）：

- `"@earendil-works/pi-ai": "0.80.5"`
- `"@earendil-works/pi-coding-agent": "0.80.5"`
- `"@earendil-works/pi-tui": "0.80.5"`

- [ ] **Step 3: 现有包 devDeps 统一为 0.80.5**

- `packages/goal/package.json`：devDependencies `"@earendil-works/pi-coding-agent": "0.79.10"` → `"0.80.5"`
- `packages/tps/package.json`：devDependencies `"@earendil-works/pi-ai": "0.74.2"` → `"0.80.5"`、`"@earendil-works/pi-coding-agent": "0.79.10"` → `"0.80.5"`
- `packages/sudo-helper/package.json`：devDependencies `"@earendil-works/pi-coding-agent": "0.79.10"` → `"0.80.5"`、`"@earendil-works/pi-tui": "0.74.2"` → `"0.80.5"`
- `packages/pi-mcp-adapter/package.json`：无需改（peer `"*"`，随根 hoist 解析 0.80.5）

- [ ] **Step 4: 重新安装并验证**

```bash
npm install && npm run check && npm test
```

预期：install 无错误（TS 6 / vitest 4 下载）；check 通过——若现有 4 包有 TS6 报错，最小修复（只改报错行，不重构）；test 无测试通过。

- [ ] **Step 5: Commit**

```bash
git add package.json package-lock.json packages/goal packages/tps packages/sudo-helper
git commit -m "chore: 工具链对齐 vitest 4 / TS 6，全仓 pi-* SDK 统一 0.80.5"
```

---

### Task 2: pi-test-utils 移植（测试夹具包）

**Files:**

- Create: `packages/pi-test-utils/{index,pi,session,contract,theme,fetch,exec,spawn,fs,manifest,concurrent-host}.ts`（自 `packages/test-utils/` 复制）
- Create: `packages/pi-test-utils/workflow-types.ts`（rpiv-workflow 类型 stub，新文件）
- Create: `packages/pi-test-utils/{manifest.test.ts,package.json,README.md,LICENSE}`
- Modify: `packages/pi-test-utils/pi.ts`、`packages/pi-test-utils/concurrent-host.ts`（workflow import 改本地）

**Interfaces:**

- Produces: `@schovest/pi-test-utils`（subpath exports `./pi` `./session` `./contract` `./theme` `./fetch` `./exec` `./spawn` `./fs` `./manifest`，同上游）；`verifyShipManifest(packageDirOrUrl)` 供各包 ship-manifest.test.ts 使用

- [ ] **Step 1: 复制并批量替换导入**

```bash
cd /data/mine/pi-package-mono
mkdir -p packages/pi-test-utils
cp /tmp/rpiv-mono/packages/test-utils/{index,pi,session,contract,theme,fetch,exec,spawn,fs,manifest,concurrent-host}.ts packages/pi-test-utils/
cp /tmp/rpiv-mono/packages/test-utils/{manifest.test.ts,README.md,LICENSE} packages/pi-test-utils/
# 全部文件批量替换包名（含注释/README/package.json）
find packages/pi-test-utils -type f \( -name "*.ts" -o -name "*.json" -o -name "*.md" \) -exec sed -i 's|@juicesharp/rpiv-|@schovest/pi-|g' {} +
```

注意：sed 会把 `@juicesharp/rpiv-workflow` 变成 `@schovest/pi-workflow`——下一步修正为本地 stub。

- [ ] **Step 2: 创建 workflow 类型 stub**

创建 `packages/pi-test-utils/workflow-types.ts`：

```ts
/**
 * 最小 rpiv-workflow 类型 stub（上游 test-utils 从 @juicesharp/rpiv-workflow
 * 类型导入 ModelSelection / WorkflowHostContext / WorkflowSessionContext；
 * rpiv-workflow 不在移植范围，这里仅提供结构兼容的本地类型，
 * 供测试夹具在 `as unknown as` 断言与选项类型中使用）。
 */

export interface ModelSelection {
  model?: string;
  thinking?: "off" | "minimal" | "low" | "medium" | "high" | "xhigh";
}

export interface WorkflowHostContext {
  cwd: string;
  hasUI: boolean;
  ui: { notify(message: string, level?: "info" | "warning" | "error"): void };
  sessionManager: {
    getBranch(): unknown;
    getSessionId(): string;
    getSessionFile(): string | undefined;
  };
  waitForIdle(): Promise<void>;
  signal?: AbortSignal;
  readonly maxConcurrency: number;
  spawnChild<T>(options: {
    prompt: string;
    model?: ModelSelection;
    signal?: AbortSignal;
    reattach?: { sessionFile: string };
    fork?: { sessionFile: string };
    unitIndex?: number;
    withSession: (child: WorkflowSessionContext) => Promise<T>;
  }): Promise<T>;
}

export interface WorkflowSessionContext extends WorkflowHostContext {
  sendUserMessage(content: string): Promise<void>;
}
```

- [ ] **Step 3: 修正两个文件的 workflow 导入**

- `packages/pi-test-utils/pi.ts`：`import type { ModelSelection, WorkflowHostContext, WorkflowSessionContext } from "@schovest/pi-workflow";` → `from "./workflow-types.js";`
- `packages/pi-test-utils/concurrent-host.ts`：同一 import → `from "./workflow-types.js";`

- [ ] **Step 4: 写 package.json（完整覆盖）**

`packages/pi-test-utils/package.json`：

```json
{
  "name": "@schovest/pi-test-utils",
  "version": "0.1.2",
  "description": "Internal test fixtures for pi-package-mono (not published)",
  "private": true,
  "type": "module",
  "license": "MIT",
  "author": "juicesharp",
  "files": [
    "index.ts", "pi.ts", "session.ts", "contract.ts", "theme.ts",
    "fetch.ts", "exec.ts", "spawn.ts", "fs.ts", "manifest.ts", "concurrent-host.ts",
    "workflow-types.ts", "README.md", "LICENSE"
  ],
  "exports": {
    ".": "./index.js", "./pi": "./pi.js", "./session": "./session.js",
    "./contract": "./contract.js", "./theme": "./theme.js", "./fetch": "./fetch.js",
    "./exec": "./exec.js", "./spawn": "./spawn.js", "./fs": "./fs.js",
    "./manifest": "./manifest.js"
  },
  "devDependencies": {
    "@earendil-works/pi-ai": "0.80.5",
    "@earendil-works/pi-coding-agent": "0.80.5",
    "@earendil-works/pi-tui": "0.80.5",
    "typebox": "^1.3.0",
    "vitest": "^4.1.10"
  },
  "peerDependencies": {
    "@earendil-works/pi-ai": "*",
    "@earendil-works/pi-coding-agent": "*",
    "@earendil-works/pi-tui": "*",
    "typebox": "*",
    "vitest": "*"
  }
}
```

- [ ] **Step 5: 运行测试**

```bash
npm install
npx vitest run packages/pi-test-utils
```

预期：manifest.test.ts 通过（`verifyShipManifest` 的 missing/stale 为空——files 列表覆盖全部 production .ts）。

- [ ] **Step 6: 全仓 check 并提交**

```bash
npm run check
git add packages/pi-test-utils
git commit -m "feat: 移植 pi-test-utils 测试夹具包（含 rpiv-workflow 类型 stub）"
```

---

### Task 3: pi-config 移植

**Files:**

- Create: `packages/pi-config/{config.ts,index.ts,config.test.ts,ship-manifest.test.ts,package.json,README.md,LICENSE}`

**Interfaces:**

- Produces: `@schovest/pi-config`（`configPath`、`loadJsonConfigWithLegacyFallback`、`validateGuidanceFields`、`GuidanceFieldsSchema` 等，导出面同上游 `index.ts`）

- [ ] **Step 1: 复制并批量替换**

```bash
cd /data/mine/pi-package-mono
mkdir -p packages/pi-config
cp /tmp/rpiv-mono/packages/rpiv-config/{config.ts,index.ts,config.test.ts,ship-manifest.test.ts,README.md,LICENSE} packages/pi-config/
find packages/pi-config -type f \( -name "*.ts" -o -name "*.json" -o -name "*.md" \) -exec sed -i 's|@juicesharp/rpiv-|@schovest/pi-|g' {} +
```

（config.ts 本身不 import @juicesharp，仅 typebox；sed 主要命中 ship-manifest.test.ts 的 test-utils 导入。）

- [ ] **Step 2: 写 package.json（完整覆盖）**

```json
{
  "name": "@schovest/pi-config",
  "version": "0.1.2",
  "description": "Shared config I/O utilities for pi-package-mono sibling packages",
  "type": "module",
  "types": "./index.ts",
  "exports": {
    ".": { "types": "./index.ts", "import": "./index.ts", "default": "./index.ts" }
  },
  "license": "MIT",
  "author": "juicesharp",
  "keywords": ["pi-package", "pi", "config", "shared-utility"],
  "files": ["index.ts", "config.ts", "README.md", "LICENSE"],
  "dependencies": { "typebox": "^1.3.0" }
}
```

- [ ] **Step 3: 运行测试并提交**

```bash
npx vitest run packages/pi-config
npm run check
git add packages/pi-config
git commit -m "feat: 移植 pi-config 共享配置包"
```

预期：config.test.ts + ship-manifest.test.ts 全绿。

---

### Task 4: pi-i18n 移植

**Files:**

- Create: `packages/pi-i18n/{i18n.ts,i18n-ui.ts,index.ts,loader.ts}` + 5 个测试 + `package.json,README.md,LICENSE`

**Interfaces:**

- Produces: `@schovest/pi-i18n`（exports `".": "./i18n.ts"`、`"./loader": "./loader.ts"`）；依赖 `@schovest/pi-config`
- Consumes: Task 3 的 `@schovest/pi-config`

- [ ] **Step 1: 复制并批量替换**

```bash
cd /data/mine/pi-package-mono
mkdir -p packages/pi-i18n
cp /tmp/rpiv-mono/packages/rpiv-i18n/{i18n.ts,i18n-ui.ts,index.ts,loader.ts,README.md,LICENSE} packages/pi-i18n/
cp /tmp/rpiv-mono/packages/rpiv-i18n/{i18n.test.ts,i18n-ui.test.ts,index.test.ts,loader.test.ts,ship-manifest.test.ts} packages/pi-i18n/
find packages/pi-i18n -type f \( -name "*.ts" -o -name "*.json" -o -name "*.md" \) -exec sed -i 's|@juicesharp/rpiv-|@schovest/pi-|g' {} +
```

验证 sed 生效：`grep -rn "@schovest/pi-config" packages/pi-i18n/i18n.ts`（应命中 import）。

- [ ] **Step 2: 写 package.json（完整覆盖）**

```json
{
  "name": "@schovest/pi-i18n",
  "version": "0.1.2",
  "description": "Pi extension. The localization foundation for rpiv-* skills: locale detection, the /languages command, the --locale flag, and a cross-package locale registry.",
  "type": "module",
  "types": "./i18n.ts",
  "exports": {
    ".": { "types": "./i18n.ts", "import": "./i18n.ts", "default": "./i18n.ts" },
    "./loader": { "types": "./loader.ts", "import": "./loader.ts", "default": "./loader.ts" }
  },
  "license": "MIT",
  "author": "juicesharp",
  "keywords": ["pi-package", "pi-extension", "pi", "i18n", "localization"],
  "files": ["index.ts", "i18n.ts", "i18n-ui.ts", "loader.ts", "README.md", "LICENSE"],
  "pi": { "extensions": ["./index.ts"] },
  "dependencies": { "@schovest/pi-config": "^0.1.2" },
  "peerDependencies": {
    "@earendil-works/pi-coding-agent": "*",
    "@earendil-works/pi-tui": "*"
  },
  "peerDependenciesMeta": {
    "@earendil-works/pi-coding-agent": { "optional": true },
    "@earendil-works/pi-tui": { "optional": true }
  }
}
```

- [ ] **Step 3: 运行测试并提交**

```bash
npx vitest run packages/pi-i18n
npm run check
git add packages/pi-i18n
git commit -m "feat: 移植 pi-i18n 本地化包"
```

预期：5 个测试全绿。若 i18n-ui.test 需要 /compat mock（本任务阶段 setup.ts 尚未含），属预期失败则在 Task 5 后重跑；否则就地修复。

---

### Task 5: test/setup.ts 裁剪重写

**Files:**

- Modify: `test/setup.ts`（重写）
- Test: 无新增——回归验证此前移植包的测试

**Interfaces:**

- Consumes: `@schovest/pi-config` 的 `configPath`；`todo.__resetState`（Task 7 加入）、`i18n.__resetState`（Task 4 已存在）
- Produces: 全仓测试环境——HOME 隔离、pi-ai 与 /compat mock、每用例清理 5 包状态与配置路径

- [ ] **Step 1: 重写 test/setup.ts**

完整覆盖（保留现有 HOME 隔离与 pi-ai mock，新增 /compat mock 与包状态清理）：

```ts
import { mkdtempSync, rmSync } from "node:fs";
import { tmpdir } from "node:os";
import { join } from "node:path";
import { beforeEach, vi } from "vitest";

const TEST_HOME = mkdtempSync(join(tmpdir(), "pi-pkg-test-home-"));
process.env.HOME = TEST_HOME;
process.env.USERPROFILE = TEST_HOME;
delete process.env.PI_CODING_AGENT_DIR;
delete process.env.XDG_CONFIG_HOME;
delete process.env.WEB_SEARCH_PROVIDER;

vi.mock("@earendil-works/pi-ai", async (importOriginal) => {
  const actual = await importOriginal<typeof import("@earendil-works/pi-ai")>();
  return {
    ...actual,
    getSupportedThinkingLevels: vi.fn(() => ["off", "minimal", "low", "medium", "high"]),
  };
});

// pi 0.80+：`completeSimple` 位于 /compat 入口；btw 的 pi-compat.ts 经
// loadCompleteSimple() 优先走 /compat，stub 需注册在这里才能被拾取。
vi.mock("@earendil-works/pi-ai/compat", async (importOriginal) => {
  const actual = await importOriginal<typeof import("@earendil-works/pi-ai/compat")>();
  return {
    ...actual,
    completeSimple: vi.fn(),
    getSupportedThinkingLevels: vi.fn(() => ["off", "minimal", "low", "medium", "high"]),
  };
});

// 包模块在 beforeEach 内动态 import（不要提升为静态 import）：
// 1. 静态 import 会先于 process.env.HOME 赋值执行，生产模块 homedir() 捕获到真实 HOME；
// 2. 测试文件的 vi.mock(node:fs) 等依赖自己是包的首次加载者，setup 静态 import 会封死 mock。
beforeEach(async () => {
  delete process.env.PI_CODING_AGENT_DIR;
  delete process.env.XDG_CONFIG_HOME;
  delete process.env.WEB_SEARCH_PROVIDER;

  // 注意：pi-todo 的 __resetState 在 Task 7 才加入此 beforeEach——pi-todo 尚未
  // 移植时动态 import 不存在的模块会让全仓 beforeEach 抛错，顺序不可提前。
  const i18n = await import("../packages/pi-i18n/i18n.js");
  i18n.__resetState();

  delete (globalThis as Record<symbol, unknown>)[Symbol.for("rpiv-btw")];
  delete (globalThis as Record<symbol, unknown>)[Symbol.for("rpiv-i18n")];

  const { configPath } = await import("@schovest/pi-config");
  const todoConfig = configPath("rpiv-todo");
  const askUserQuestionConfig = configPath("rpiv-ask-user-question");
  const i18nConfig = configPath("rpiv-i18n", "locale.json");
  rmSync(todoConfig, { force: true });
  rmSync(askUserQuestionConfig, { force: true });
  rmSync(i18nConfig, { force: true });
  rmSync(join(process.env.HOME!, ".pi", "agent"), { recursive: true, force: true });
});
```

注意：`configPath` 第二参数形态以上游 `rpiv-config/config.ts` 实际签名为准（实现时先读 `packages/pi-config/config.ts` 确认，若签名不同按实际修正此调用）。

- [ ] **Step 2: 运行全部现有测试**

```bash
npx vitest run packages/pi-config packages/pi-i18n packages/pi-test-utils
npm run check
```

预期：全绿（Task 4 若曾有挂起失败，此时应通过）。

- [ ] **Step 3: Commit**

```bash
git add test/setup.ts
git commit -m "test: 重写 setup.ts——pi-ai/compat mock + todo/i18n 状态重置 + 配置路径清理"
```

---

### Task 6: pi-btw 移植

**Files:**

- Create: `packages/pi-btw/{index.ts,btw.ts,btw-budget.ts,btw-messages.ts,btw-ui.ts,pi-compat.ts}` + `prompts/btw-system.txt` + 6 测试 + `package.json,README.md,LICENSE`
- Create: `packages/pi-btw/docs/`（复制上游 docs 的 .md，跳过图片）

**Interfaces:**

- Produces: `@schovest/pi-btw`（pi.extensions 入口 `./index.ts`；`/btw` 命令）
- Consumes: 无 @schovest 依赖；peer + devDeps pi-* 0.80.5

- [ ] **Step 1: 复制、批量替换、裁剪 docs 图片**

```bash
cd /data/mine/pi-package-mono
mkdir -p packages/pi-btw
cp /tmp/rpiv-mono/packages/rpiv-btw/{index.ts,btw.ts,btw-budget.ts,btw-messages.ts,btw-ui.ts,pi-compat.ts,README.md,LICENSE} packages/pi-btw/
mkdir -p packages/pi-btw/prompts packages/pi-btw/docs
cp /tmp/rpiv-mono/packages/rpiv-btw/prompts/btw-system.txt packages/pi-btw/prompts/
cp /tmp/rpiv-mono/packages/rpiv-btw/docs/*.md packages/pi-btw/docs/ 2>/dev/null || true
cp /tmp/rpiv-mono/packages/rpiv-btw/{btw-budget.test.ts,btw.command.test.ts,btw.test.ts,btw-ui.test.ts,pi-compat.test.ts,ship-manifest.test.ts} packages/pi-btw/
find packages/pi-btw -type f \( -name "*.ts" -o -name "*.json" -o -name "*.md" -o -name "*.txt" \) -exec sed -i 's|@juicesharp/rpiv-|@schovest/pi-|g' {} +
```

验证：`grep -rn "juicesharp" packages/pi-btw --include="*.ts"` 应无命中（README 里的 GitHub 链接除外）。

- [ ] **Step 2: 写 package.json（完整覆盖）**

```json
{
  "name": "@schovest/pi-btw",
  "version": "0.1.2",
  "description": "Pi extension. The /btw slash command, for putting a one-off side question to the same primary model without polluting the main conversation.",
  "type": "module",
  "types": "./index.ts",
  "exports": {
    ".": { "types": "./index.ts", "import": "./index.ts", "default": "./index.ts" }
  },
  "license": "MIT",
  "author": "juicesharp",
  "keywords": ["pi-package", "pi-extension", "pi", "btw", "side-question", "overlay"],
  "files": [
    "index.ts", "btw.ts", "btw-budget.ts", "btw-messages.ts", "btw-ui.ts", "pi-compat.ts",
    "prompts/", "docs/", "README.md", "LICENSE"
  ],
  "pi": { "extensions": ["./index.ts"] },
  "devDependencies": {
    "@earendil-works/pi-ai": "0.80.5",
    "@earendil-works/pi-coding-agent": "0.80.5",
    "@earendil-works/pi-tui": "0.80.5"
  },
  "peerDependencies": {
    "@earendil-works/pi-ai": "*",
    "@earendil-works/pi-coding-agent": "*",
    "@earendil-works/pi-tui": "*"
  },
  "peerDependenciesMeta": {
    "@earendil-works/pi-ai": { "optional": true },
    "@earendil-works/pi-coding-agent": { "optional": true },
    "@earendil-works/pi-tui": { "optional": true }
  }
}
```

- [ ] **Step 3: 运行测试、check、提交**

```bash
npx vitest run packages/pi-btw
npm run check
git add packages/pi-btw
git commit -m "feat: 移植 pi-btw——/btw 侧问命令"
```

预期：6 测试全绿（pi-compat.test 验证 /compat 与根入口回退链，依赖 Task 5 的 /compat mock）。

---

### Task 7: pi-todo 移植

**Files:**

- Create: `packages/pi-todo/`（全部上游文件：4 根 ts + state/ 8 ts + tool/ 2 ts + view/ 1 ts + locales/ 9 json + 19 测试 + package.json/README/LICENSE/docs）

**Interfaces:**

- Consumes: `@schovest/pi-config`（config.ts 依赖）、软可选 `@schovest/pi-i18n`（动态 import，index.ts 与 state/i18n-bridge.ts）
- Produces: `@schovest/pi-todo`（`todo` 工具、`/todos` 命令、覆盖层；导出 `__resetState` 供 setup.ts）

- [ ] **Step 1: 复制整个包目录并批量替换**

```bash
cd /data/mine/pi-package-mono
mkdir -p packages/pi-todo
cp -r /tmp/rpiv-mono/packages/rpiv-todo/. packages/pi-todo/
rm -rf packages/pi-todo/docs/*.png packages/pi-todo/docs/*.jpg packages/pi-todo/docs/*.svg packages/pi-todo/docs/cover.* 2>/dev/null || true
find packages/pi-todo -type f \( -name "*.ts" -o -name "*.json" -o -name "*.md" \) -exec sed -i 's|@juicesharp/rpiv-|@schovest/pi-|g' {} +
```

验证：`grep -rn "@juicesharp" packages/pi-todo --include="*.ts"` 无命中；`grep -rn "@schovest/pi-i18n" packages/pi-todo/index.ts` 命中动态 import。

- [ ] **Step 2: 写 package.json（完整覆盖）**

```json
{
  "name": "@schovest/pi-todo",
  "version": "0.1.2",
  "description": "Pi extension. A todo list for the model, rendered as a live overlay that survives /reload and conversation compaction.",
  "type": "module",
  "types": "./index.ts",
  "exports": {
    ".": { "types": "./index.ts", "import": "./index.ts", "default": "./index.ts" }
  },
  "license": "MIT",
  "author": "juicesharp",
  "keywords": ["pi-package", "pi-extension", "pi", "todo"],
  "files": [
    "config.ts", "index.ts", "locales/", "todo.ts", "todo-overlay.ts", "LICENSE", "docs/", "README.md",
    "state/invariants.ts", "state/i18n-bridge.ts", "state/replay.ts", "state/selectors.ts",
    "state/state-reducer.ts", "state/state.ts", "state/store.ts", "state/task-graph.ts",
    "tool/response-envelope.ts", "tool/types.ts", "view/format.ts"
  ],
  "pi": { "extensions": ["./index.ts"] },
  "dependencies": {
    "@schovest/pi-config": "^0.1.2",
    "typebox": "^1.3.0"
  },
  "devDependencies": {
    "@earendil-works/pi-ai": "0.80.5",
    "@earendil-works/pi-coding-agent": "0.80.5",
    "@earendil-works/pi-tui": "0.80.5"
  },
  "peerDependencies": {
    "@earendil-works/pi-ai": "*",
    "@earendil-works/pi-coding-agent": "*",
    "@earendil-works/pi-tui": "*"
  },
  "peerDependenciesMeta": {
    "@earendil-works/pi-ai": { "optional": true },
    "@earendil-works/pi-coding-agent": { "optional": true },
    "@earendil-works/pi-tui": { "optional": true }
  }
}
```

注意：`@schovest/pi-i18n` 软可选依赖与上游一致放在 **peerDependenciesMeta optional**（上游即如此，npm 不会自动装；缺省时 i18n-bridge 回退英文）。若上游 todo package.json 里还有 `"@schovest/pi-i18n"` peer，补进 peerDependencies。

- [ ] **Step 3: 在 setup.ts 加入 todo 状态重置**

编辑 `test/setup.ts` 的 beforeEach：删除 `// 注意：pi-todo 的 __resetState 在 Task 7 才加入...` 注释，并在 `const i18n = await import(...)` 之前插入：

```ts
  const todo = await import("../packages/pi-todo/todo.js");
  todo.__resetState();
```

这样 pi-todo 测试获得跨用例 store 隔离（上游 setup 同款行为），其他包不受影响。

- [ ] **Step 4: 运行测试、check、提交**

```bash
npx vitest run packages/pi-todo
npm run check
git add packages/pi-todo
git commit -m "feat: 移植 pi-todo——todo 工具 + /todos 命令 + 实时覆盖层"
```

预期：19 测试全绿（含 replay/compaction 存活、overlay 生命周期、i18n shim 回退）。同时重跑 Task 3-6 各包测试确认 setup 改动无回归：`npx vitest run packages/pi-config packages/pi-i18n packages/pi-test-utils packages/pi-btw`。

---

### Task 8: pi-ask-user-question 移植

**Files:**

- Create: `packages/pi-ask-user-question/`（全部上游文件：6 根 ts + state/ 12 ts + tool/ 4 ts + view/ 16 ts + locales/ 9 json + test-fixtures.ts + 32 测试 + package.json/README/LICENSE/docs）

**Interfaces:**

- Consumes: `@schovest/pi-config`、软可选 `@schovest/pi-i18n`
- Produces: `@schovest/pi-ask-user-question`（exports `"."` 与 `"./events"`；`ask_user_question` 工具）

- [ ] **Step 1: 复制整个包目录并批量替换**

```bash
cd /data/mine/pi-package-mono
mkdir -p packages/pi-ask-user-question
cp -r /tmp/rpiv-mono/packages/rpiv-ask-user-question/. packages/pi-ask-user-question/
rm -rf packages/pi-ask-user-question/docs/*.png packages/pi-ask-user-question/docs/*.jpg packages/pi-ask-user-question/docs/*.svg 2>/dev/null || true
find packages/pi-ask-user-question -type f \( -name "*.ts" -o -name "*.json" -o -name "*.md" \) -exec sed -i 's|@juicesharp/rpiv-|@schovest/pi-|g' {} +
```

验证：`grep -rn "@juicesharp" packages/pi-ask-user-question --include="*.ts"` 无命中。

- [ ] **Step 2: 写 package.json（完整覆盖）**

```json
{
  "name": "@schovest/pi-ask-user-question",
  "version": "0.1.2",
  "description": "Pi extension. A structured questionnaire the model can put to you when it would otherwise guess, with typed options instead of free-form replies.",
  "type": "module",
  "types": "./index.ts",
  "exports": {
    ".": { "types": "./index.ts", "import": "./index.ts", "default": "./index.ts" },
    "./events": { "types": "./events.ts", "import": "./events.ts", "default": "./events.ts" }
  },
  "license": "MIT",
  "author": "juicesharp",
  "keywords": ["pi-package", "pi-extension", "pi"],
  "files": [
    "ask-user-question.ts", "rpc-fallback.ts", "config.ts", "events.ts", "index.ts", "reconcile.ts",
    "LICENSE", "locales/", "docs/", "README.md",
    "state/build-questionnaire.ts", "state/external-editor.ts", "state/i18n-bridge.ts",
    "state/key-router.ts", "state/questionnaire-session.ts", "state/row-intent.ts",
    "state/selectors/contract.ts", "state/selectors/derivations.ts", "state/selectors/focus.ts",
    "state/selectors/projections.ts", "state/state-reducer.ts", "state/state.ts",
    "tool/format-answer.ts", "tool/response-envelope.ts", "tool/types.ts", "tool/validate-questionnaire.ts",
    "view/component-binding.ts", "view/components/inline-input.ts", "view/components/multi-select-view.ts",
    "view/components/option-list-view.ts", "view/components/preview/markdown-content-cache.ts",
    "view/components/preview/preview-block-renderer.ts", "view/components/preview/preview-box-renderer.ts",
    "view/components/preview/preview-layout-decider.ts", "view/components/preview/preview-pane.ts",
    "view/components/submit-picker.ts", "view/components/tab-bar.ts", "view/components/wrapping-select.ts",
    "view/dialog-builder.ts", "view/props-adapter.ts", "view/stateful-view.ts", "view/tab-components.ts",
    "view/tab-content-strategy.ts"
  ],
  "pi": { "extensions": ["./index.ts"] },
  "dependencies": {
    "@schovest/pi-config": "^0.1.2",
    "typebox": "^1.3.0"
  },
  "devDependencies": {
    "@earendil-works/pi-ai": "0.80.5",
    "@earendil-works/pi-coding-agent": "0.80.5",
    "@earendil-works/pi-tui": "0.80.5"
  },
  "peerDependencies": {
    "@earendil-works/pi-coding-agent": "*",
    "@earendil-works/pi-tui": "*"
  },
  "peerDependenciesMeta": {
    "@earendil-works/pi-coding-agent": { "optional": true },
    "@earendil-works/pi-tui": { "optional": true }
  }
}
```

注意：上游 ask-user-question 的 peer 含 `@juicesharp/rpiv-i18n`（optional），sed 后为 `@schovest/pi-i18n`——按上游原样补进 peerDependencies + peerDependenciesMeta optional。

- [ ] **Step 3: 运行测试、check、提交**

```bash
npx vitest run packages/pi-ask-user-question
npm run check
git add packages/pi-ask-user-question
git commit -m "feat: 移植 pi-ask-user-question——ask_user_question 分页签对话框"
```

预期：32 测试全绿。

---

### Task 9: 全仓验证与收尾

**Files:**

- Modify: `README.md`（仓库根——包列表补 6 个新包）

- [ ] **Step 1: 全仓 check + 全量测试**

```bash
cd /data/mine/pi-package-mono && npm run check && npm test
```

预期：`npm run check` 零错误（biome 1.9.4 将上游 tab 缩进统一格式化为 2 空格）；`npm test` 全绿——测试计数 = 65（btw 6 / todo 19 / ask-user-question 32 / config 2 / i18n 5 / test-utils 1）。若个别失败：先单文件重跑（`npx vitest run packages/<pkg>`）确认为残留状态泄漏还是真实失败，按系统化调试修复。

- [ ] **Step 2: 发布干跑**

```bash
npm run publish:dry
```

预期：5 个非 private 包（pi-btw/pi-todo/pi-ask-user-question/pi-config/pi-i18n）各自执行 prepublishOnly（check）+ dry-run 成功；pi-test-utils 被 npm 跳过（private）。

- [ ] **Step 3: 更新根 README 包表**

`README.md` 的包表格新增 6 行（名称 / 用途 / npm 链接 `https://www.npmjs.com/package/@schovest/pi-*`），说明随附"基于 @juicesharp/rpiv-* 2.4.0 移植"。

- [ ] **Step 4: 最终提交**

```bash
git add README.md && git commit -m "docs: README 补充 6 个移植包"
```

---

## Self-Review 记录（实现者/审查者核对）

- **Spec 覆盖**：6 包 ✓（Task 2-8）、命名替换 ✓（各 Task Step 1）、workflow stub ✓（Task 2 Step 2-3）、setup 裁剪 ✓（Task 5，todo reset 增量加入在 Task 7 Step 3）、工具链对齐 ✓（Task 1）、SDK 0.80.5 ✓（Task 1）、保留 .js 后缀 ✓（不 sed 相对路径）、保留 rpiv-* 字面量 ✓（Global Constraints）、测试搬运 ✓（各 Task）、publish:dry ✓（Task 9）
- **占位符**：无 TBD/TODO；每 Task 有精确命令与完整 package.json
- **类型一致性**：`configPath` 签名以 Task 5 Step 1 末尾"实现时先读 config.ts 确认"为准；`__resetState` 由 todo.ts/index.ts（`export { __resetState, ... } from "./state/store.js"`）与 i18n.ts 导出（已核实）
- **已知风险**：① TS 6.0.3 对现有 4 包报错 → Task 1 最小修复；② 上游 package.json peer 字段细节（如 todo 的 rpiv-i18n peer）与上文模板可能不完全一致 → 以 sed 后的上游 package.json 为基准补字段，模板为底线；③ 若 npm 无法解析 `vitest ^4.1.10` / `typescript ^6.0.3`（上游真实 pin，应存在）→ 先 `npm view` 确认版本号再装
